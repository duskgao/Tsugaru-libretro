# FMTOWNS libretro core 使用说明书

> 写给完全不懂编程的你。一步一步照做即可。
> 这是一个把「FM Towns 电脑模拟器（Tsugaru）」变成 RetroArch 核心（core）的项目。
> 你手上的所有东西，最终都产出一个文件：`towns_libretro.dll`（Windows 用的核心），
> 把它放进 RetroArch 就能玩 FM Towns 游戏。

---

## 0. 一句话总结

- 母体（模拟器本体）：`TOWNSEMU`（津軽 / Tsugaru），作者 CaptainYS。
- 我们的工作：在它上面加一层「libretro 接口」，让 RetroArch 能调用它。
- 产出物：`towns_libretro.dll`，放到 RetroArch 的 `cores/` 目录即可。
- 代码托管：GitHub 仓库 `duskgao/Tsugaru-libretro`，分支名 `libretro`。

---

## 1. 需要用到的工具（只装一次）

| 工具 | 作用 | 在哪 |
|------|------|------|
| RetroArch | 运行核心的软件（前端） | `E:\RetroArch\RetroArch-Win64` |
| MSYS2 | 编译代码的工具链 | `C:\msys64` |
| Git | 管理代码版本 | 系统自带 / 已装 |
| v2rayN | 网络代理（访问 GitHub 用） | 你已有 |

> 检查 MSYS2 是否正常：打开 `C:\msys64\msys2.exe`，输入 `gcc --version` 能显示版本即可。

---

## 2. 项目目录结构（都认识一下）

```
E:\FMTOWNS\                  ← 项目主目录（就是本目录）
│
├── build_libretro_win.ps1   ← 一键构建脚本（自动编译 + 部署到 RetroArch）
│
├── src\                     ← 全部源码（来自上游 TOWNSEMU + 我们的移植）
│   ├── main_libretro\       ← ★ 我们的移植代码全在这
│   │   ├── libretro.cpp     ←    核心主程序（改这里最重要）
│   │   ├── keymap.h         ←    键盘映射表
│   │   ├── towns_libretro.info ← 核心描述文件
│   │   ├── README.md        ←    移植技术说明
│   │   ├── libretro.h       ←    libretro 官方接口定义
│   │   └── libretro.def     ←    Windows 导出符号
│   ├── towns\               ←    模拟器本体代码（一般不用动）
│   └── ...                  ←    其他底层模块
│
├── src\build_libretro\main_libretro\
│   └── towns_libretro.dll   ← ★★ 编译产物（最终成果）
│
└── LIBRETRO_PORT.md         ← 移植全过程的技术笔记（想深究再看）
```

---

## 3. 怎么编译（生成 DLL）

### 方法一：一键脚本（推荐，最省事）

1. **先彻底关掉 RetroArch**（它运行时会锁住 DLL，导致复制失败）。
2. 打开 PowerShell（开始菜单搜 PowerShell）。
3. 输入下面命令回车：

```powershell
powershell -ExecutionPolicy Bypass -File E:\FMTOWNS\build_libretro_win.ps1
```

4. 看到绿色 `==> Deployed OK` 就成功了。
   它会自动：编译 → 把 DLL 复制到 RetroArch 的 `cores/` → 把 info 复制到 `info/`。

> 只想编译不想自动部署，加 `-NoDeploy` 参数。

### 方法二：手动编译（想自己控制时）

打开 MSYS2 MinGW64 终端，输入：

```bash
cd /e/FMTOWNS/src/main_libretro
export PATH=/c/msys64/mingw64/bin:/usr/bin:$PATH
sh build_libretro.sh
```

产物会出现在 `E:\FMTOWNS\src\build_libretro\main_libretro\towns_libretro.dll`。

---

## 4. 怎么部署到 RetroArch（手动方式）

1. 彻底退出 RetroArch。
2. 复制 `towns_libretro.dll` → RetroArch 的 `cores\` 目录。
3. 复制 `towns_libretro.info` → RetroArch 的 `info\` 目录。
4. 把 BIOS 文件放进 RetroArch 的 `system\` 目录（至少需要 `FMT_SYS.ROM`）。
5. 启动 RetroArch → 主菜单「加载内容 Load Content」→ 选一个 `.cue` 镜像 → 选 `Tsugaru` 核心。

> BIOS（`FMT_SYS.ROM` 等）来自真实 FM Towns 硬件或合法备份，本项目不附带。

---

## 5. 你的核心长什么样（成果自检）

编译出的 `towns_libretro.dll` 应该是「单文件自包含」的：
- 只依赖 Windows 自带库（`KERNEL32.dll`、`msvcrt.dll`、`WS2_32.dll`）。
- **不依赖任何第三方 DLL**（没有 libwinpthread、没有 SDL）。

检查方法（在 PowerShell）：
```powershell
& "C:\msys64\mingw64\bin\objdump.exe" -p "E:\FMTOWNS\src\build_libretro\main_libretro\towns_libretro.dll" | Select-String "DLL Name"
```
应该只看到上面那 3 个系统 DLL。

---

## 6. 最重要的日常工作：上游模拟器更新了怎么办

这是你提的重点。原模拟器（TOWNSEMU）会不断更新，你要把新代码拉回来、合并、再推回去。

### 6.1 仓库布局（先理解）

- `origin`   = 上游 `captainys/TOWNSEMU`（官方，只读参考）
- `duskgao`  = 你自己的 `duskgao/Tsugaru-libretro`（要推送的目标）
- 本地有两条分支：
  - `master`   = 和上游完全一致（相当于"上游的镜子"）
  - `libretro` = 我们的移植分支（真正干活的分支）

### 6.2 标准更新流程（照抄）

在 PowerShell，进入目录后：

```powershell
cd E:\FMTOWNS

# 1. 先切到 libretro 分支（我们干活的地方）
git checkout libretro

# 2. 把上游最新代码拉到本地（origin = 上游）
git fetch origin

# 3. 把上游的 master 合并进我们的 libretro 分支
git merge origin/master

# 4. 如果有冲突（两边改了同一个文件），解决后重新提交
#    —— 一般冲突只发生在 src/towns 里，src/main_libretro 是我们独有的不会冲突

# 5. 重新编译，确认没问题
powershell -ExecutionPolicy Bypass -File E:\FMTOWNS\build_libretro_win.ps1

# 6. 把成果推回你自己的 GitHub
git push duskgao libretro
```

> 网络问题（git 卡住/超时）：先确认 v2rayN 开着，Git 已配置走代理（见 8.2 节）。

---

## 7. 怎么发布一个 Release（给用户下载 DLL）

当你编译出一个干净的成果、想放上 GitHub 让大家下载：

1. **先打一个版本标签（tag）**。版本号规则建议 `v20260522` 这种（跟上游版本号对应）。

```powershell
cd E:\FMTOWNS
git tag v20260522
git push duskgao v20260522
```

2. **去 GitHub 网页创建 Release**：
   - 打开 `https://github.com/duskgao/Tsugaru-libretro`
   - 点右侧 `Releases` → `Draft a new release`
   - 「Choose a tag」选刚推的 `v20260522`
   - 标题写 `v20260522`，说明写本次改了什么
   - 把 `towns_libretro.dll` 拖进附件框上传
   - 点 `Publish release`

> 这样别人就能在 GitHub 上直接下载 DLL 用，不用自己编译。

---

## 8. Git 常用救命命令

### 8.1 查看当前状态
```powershell
git status              # 看哪些文件改动了
git log --oneline -5    # 看最近 5 次提交
git branch -a           # 看所有分支
```

### 8.2 配置代理（v2rayN，端口 10808）
```powershell
git config --global http.proxy http://127.0.0.1:10808
git config --global https.proxy http://127.0.0.1:10808
# 取消代理：
git config --global --unset http.proxy
git config --global --unset https.proxy
```

### 8.3 提交改动（改完代码后）
```powershell
git add -A
git commit -m "说明你改了什么"
git push duskgao libretro
```

### 8.4 撤销一个未提交的改动
```powershell
git checkout -- <文件名>   # 放弃某个文件的修改
```

---

## 9. libretro-super 官方发布（让核心进入 libretro 官方库）

> 这一步是要把核心"官宣"进 libretro 生态，让所有平台的 RetroArch 都能一键下载。
> 它是给全世界用的终极目标，流程较长，需要耐心。

要点（负责人对照 check）：
1. 核心必须是「官方规范」的：`.info` 文件完整、`retro_*` 接口符合规范、声明支持无内容运行等。
2. 需要给 `libretro-super` 提交一个 recipe（构建脚本），官方 buildbot 才会自动为
   Windows/Linux/macOS/Android 等所有平台编译。
3. recipe 通常需要核心仓库命名规范，并且经常要求核心已成熟稳定。

> 这条是进阶流程，等核心功能稳定后再做，遇到不确定的再逐项确认。

---

## 9.5 游戏存档怎么处理（重要）

不同游戏在 FM Towns 上的存档方式不同，核心做了三类支持，**关键取决于游戏**：

### (1) CD 游戏需要"存档硬盘"（最常见，如 3x3 Eyes）
很多 CD 游戏会把进度写到一块**虚拟硬盘**上。核心现在支持你在 option 里挂载一块
可写硬盘，与 CD 并存：

1. **先造一个空硬盘镜像**（一个足够大的空文件即可，核心能识别任意非空镜像）。
   在 PowerShell：
   ```powershell
   # 造一个 32MB 的空硬盘文件（全零）
   $f = New-Object System.IO.FileStream("E:\RetroArch\RetroArch-Win64\system\3x3_hdd.img",[System.IO.FileMode]::Create)
   $f.SetLength(32MB); $f.Close()
   ```
2. 启动 RetroArch，加载游戏前，进 **设置 → Core Options**（或加载后按
   Quick Menu → Core Options），把 **`Extra SCSI Hard Disk Image (full path)`**
   设为上面那个文件的完整路径，例如 `E:\RetroArch\RetroArch-Win64\system\3x3_hdd.img`。
3. 再加载 3x3 的 `.cue`。现在游戏就能往硬盘写存档了。

> 硬盘镜像是**直接读写文件**的，游戏写多少就落到文件里多少，退出后仍在。
> 不需要额外"保存"操作。

### (2) 软盘游戏存档（现在会自动写回）
软盘镜像的改动（比如你在软盘里存了档）会**每虚拟秒自动写回镜像文件**，退出时
也会兜底保存一次。以前退出会丢存档，现在不会了。

### (3) 电池存档 / CMOS（现在会自动写回）
系统 CMOS（保存日期、设置、部分存档）在退出时会自动写回
`<RetroArch saves>\towns_cmos.bin`。请确保 RetroArch 的 **Savefile Directory**
有写权限。

### (4) 即时存档（RetroArch 的保存状态）
核心已支持 `retro_serialize`/`retro_unserialize`，RetroArch 的
「保存状态 / 加载状态」可用。即时存档会包含内存、CMOS、软盘数据，但**不包含
硬盘内容**（硬盘靠镜像文件本身持久化，无需即时存档备份）。

---

## 10. 常见问题

**Q：编译报错 `cc1plus` 找不到 / PATH 问题？**
A：确保用 MinGW64 终端，且先执行 `export PATH=/c/msys64/mingw64/bin:/usr/bin:$PATH`。

**Q：部署后 RetroArch 还是旧核心 / 改动没生效？**
A：RetroArch 会锁住 `cores/*.dll`。**完全退出 RetroArch** 再复制，否则复制会静默失败。
备份文件是 `towns_libretro.dll.bak`，可对照确认是不是被覆盖。

**Q：git push 报 `did not receive expected object`？**
A：说明本地和远端历史不一致。用 `git filter-branch` 做成单一历史，或新建孤儿分支再推。

**Q：RetroArch 加载核心卡死？**
A：确认 core 里第一个无条件调用 `RETRO_ENVIRONMENT_SET_SUPPORT_NO_GAME`，否则 RA 会卡。

---

*本说明书由 AI 辅助整理，针对"不懂编程也能操作"的目标编写。遇到任何步骤卡住，把报错原文发给你的 AI 助手。*
